'use client'
import { useEffect,useState } from 'react'
import toast from 'react-hot-toast'
import { RefreshCw, ShoppingBag } from 'lucide-react'
import { PageHeader } from '@/components/OmniPage'
import { ActionButton, api, DataState, MetricGrid, StatusBadge, Toolbar } from '@/components/PlatformUI'
export default function ShopifyPage(){const [status,setStatus]=useState<any>({});const [products,setProducts]=useState<any[]>([]);const [search,setSearch]=useState('');const [loading,setLoading]=useState(true);const [error,setError]=useState('');const load=async()=>{setLoading(true);setError('');try{const s=await api('/api/platform/shopify/status');setStatus(s.data);if(s.data.connected){const p=await api(`/api/platform/shopify/products?search=${encodeURIComponent(search)}`);setProducts(p.data)}}catch(e){setError(e instanceof Error?e.message:'Load failed')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const sync=async(scope='all')=>{try{await api(`/api/platform/shopify/sync?scope=${scope}`,{method:'POST'});toast.success(`${scope} sync completed`);load()}catch(e){toast.error(e instanceof Error?e.message:'Sync failed')}};return <div className="space-y-7"><PageHeader title="Shopify Brain" description="Live commerce data for both agents: products, variants, prices, inventory, orders, customers, and sync health. No static catalog training."/><Toolbar search={search} onSearch={setSearch} onRefresh={load} onExport={()=>window.print()}><ActionButton primary onClick={()=>sync()} icon={<RefreshCw className="h-4 w-4"/>}>Sync now</ActionButton><ActionButton onClick={()=>sync('products')}>Products</ActionButton><ActionButton onClick={()=>sync('orders')}>Orders</ActionButton></Toolbar><MetricGrid items={[{label:'Connection',value:status.connected?'Live':'Not configured'},{label:'API status',value:status.api_status||status.mode||'—'},{label:'Products fetched',value:status.products_count||products.length},{label:'Store',value:status.store_domain||'Add credentials'}]}/><DataState loading={loading} error={error} empty={status.connected&&!products.length} onRetry={load}>{!status.connected?<div className="surface-panel rounded-[28px] p-8 text-center"><ShoppingBag className="mx-auto h-9 w-9 text-[#68d2c8]"/><div className="mt-4 text-lg font-bold text-white">Connect Shopify in environment settings</div><p className="mx-auto mt-2 max-w-xl text-sm text-slate-500">Set SHOPIFY_STORE_DOMAIN and SHOPIFY_ACCESS_TOKEN on the backend. The UI will immediately switch to live product, inventory, order, and customer access.</p></div>:<div className="surface-panel overflow-hidden rounded-[24px]"><table className="w-full text-left text-sm"><thead className="border-b border-white/8 text-xs uppercase text-slate-500"><tr><th className="p-4">Product</th><th>Status</th><th>Variants</th><th>Price</th><th>Inventory</th></tr></thead><tbody>{products.map(p=><tr key={p.id} className="border-b border-white/6"><td className="p-4"><div className="font-semibold text-white">{p.title}</div><div className="text-xs text-slate-500">{p.vendor||'No vendor'} · {p.product_type||'Uncategorized'}</div></td><td><StatusBadge value={p.status}/></td><td>{p.variants?.length||0}</td><td>{p.variants?.[0]?.price||'—'}</td><td>{p.variants?.reduce((n:number,v:any)=>n+(v.inventory_quantity||0),0)||0}</td></tr>)}</tbody></table></div>}</DataState></div>}
