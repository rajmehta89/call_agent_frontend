'use client'
import { useEffect,useState } from 'react'
import { Activity, Bot, MessageSquare, PhoneCall, ShoppingBag } from 'lucide-react'
import { ChannelCard, PageHeader } from '@/components/OmniPage'
import { api, DataState, MetricGrid, Toolbar } from '@/components/PlatformUI'
export default function Dashboard(){const [data,setData]=useState<any>({});const [loading,setLoading]=useState(true);const [error,setError]=useState('');const load=()=>{setLoading(true);setError('');api('/api/platform/dashboard').then(p=>setData(p.data)).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);const m=data.metrics||{};return <div className="space-y-7"><PageHeader title="Omnichannel Command Center" description="One operating view for WhatsApp, Voice, customers, leads, Shopify, automation, human handoff, and the shared AI Brain."/><Toolbar onRefresh={load} onExport={()=>window.print()}><input type="date" className="w-auto"/><select className="w-auto"><option>All channels</option><option>WhatsApp</option><option>Voice</option></select><select className="w-auto"><option>All agents</option></select></Toolbar><DataState loading={loading} error={error} onRetry={load}><MetricGrid items={[{label:'Total conversations',value:m.total_conversations||0},{label:'WhatsApp',value:m.whatsapp_conversations||0},{label:'Voice calls',value:m.voice_calls||0},{label:'Customers',value:m.customers||0},{label:'Total leads',value:m.total_leads||0},{label:'Qualified leads',value:m.qualified_leads||0},{label:'AI resolution',value:`${m.ai_resolution_percent||0}%`},{label:'Human handoff',value:`${m.human_handoff_percent||0}%`},{label:'Shopify enquiries',value:m.shopify_product_enquiries||0},{label:'Orders influenced',value:m.orders_influenced||0},{label:'AI usage',value:m.ai_usage||0},{label:'Errors',value:m.error_count||0}]}/><div className="mt-6 grid gap-5 lg:grid-cols-3"><ChannelCard title="WhatsApp Agent" description="Messaging, customer context, live Shopify answers, lead capture, and human takeover." status={data.channels?.whatsapp?.connected?'Connected':'Not connected'} icon={MessageSquare}/><ChannelCard title="Voice Agent" description="Inbound and outbound calls, transcripts, summaries, leads, and team transfers." status={data.channels?.voice?.connected?'Connected':'Not connected'} icon={PhoneCall}/><ChannelCard title="Shared AI Brain" description="One knowledge, customer memory, Shopify, tool permission, and activity layer." status="Active" icon={Bot}/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><section className="surface-panel rounded-[28px] p-6"><div className="flex items-center gap-2 font-bold text-white"><Activity className="h-4 w-4 text-[#68d2c8]"/>Recent activity</div><div className="mt-4 divide-y divide-white/6">{(data.recent_activity||[]).length?(data.recent_activity||[]).map((r:any)=><div key={r._id} className="py-4"><div className="flex justify-between gap-3"><span className="text-sm font-medium text-white">{r.channel} · {r.agent}</span><span className="text-xs text-slate-600">{new Date(r.created_at).toLocaleString()}</span></div><p className="mt-1 line-clamp-1 text-sm text-slate-500">{r.request}</p></div>):<div className="py-10 text-center text-sm text-slate-500">AI activity appears after the first customer interaction.</div>}</div></section><section className="surface-panel rounded-[28px] p-6"><div className="flex items-center gap-2 font-bold text-white"><ShoppingBag className="h-4 w-4 text-[#d6a34f]"/>Shopify commerce layer</div><div className="mt-5 text-3xl font-extrabold text-white">{data.shopify?.connected?'Live':'Not connected'}</div><p className="mt-3 text-sm leading-6 text-slate-500">{data.shopify?.connected?`Live queries use ${data.shopify.store_domain}.`:'Connect Shopify to unlock product, inventory, order, and customer tools for both agents.'}</p></section></div></DataState></div>}
