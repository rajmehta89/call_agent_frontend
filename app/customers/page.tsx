'use client'
import Link from 'next/link'
import { useEffect,useState } from 'react'
import toast from 'react-hot-toast'
import { Plus } from 'lucide-react'
import { PageHeader } from '@/components/OmniPage'
import { ActionButton, api, DataState, downloadCsv, StatusBadge, Toolbar } from '@/components/PlatformUI'
export default function CustomersPage(){const [rows,setRows]=useState<any[]>([]);const [search,setSearch]=useState('');const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [show,setShow]=useState(false);const [form,setForm]=useState({name:'',phone:'',email:'',location:'',tags:[],notes:''});const load=()=>{setLoading(true);api(`/api/platform/customers?search=${encodeURIComponent(search)}`).then(p=>setRows(p.data)).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);const create=async()=>{try{await api('/api/platform/customers',{method:'POST',body:JSON.stringify(form)});toast.success('Customer created');setShow(false);load()}catch(e){toast.error(e instanceof Error?e.message:'Create failed')}};return <div className="space-y-7"><PageHeader title="Customers" description="Unified customer profiles joining WhatsApp history, Voice calls, Shopify activity, lead status, notes, tags, and AI interactions."/><Toolbar search={search} onSearch={setSearch} onRefresh={load} onExport={()=>downloadCsv('customers')}><ActionButton primary onClick={()=>setShow(!show)} icon={<Plus className="h-4 w-4"/>}>Add customer</ActionButton><ActionButton>Import CSV</ActionButton><ActionButton>Bulk tag</ActionButton></Toolbar>{show&&<div className="surface-panel grid gap-4 rounded-[24px] p-5 md:grid-cols-3"><input placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Phone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input placeholder="Location" value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/><textarea className="md:col-span-2" placeholder="Notes" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/><ActionButton primary onClick={create}>Create customer</ActionButton></div>}<DataState loading={loading} error={error} empty={!rows.length} onRetry={load}><div className="surface-panel overflow-x-auto rounded-[24px]"><table className="w-full min-w-[820px] text-left text-sm"><thead className="border-b border-white/8 text-xs uppercase text-slate-500"><tr><th className="p-4"><input type="checkbox" className="h-4 w-4"/></th><th>Name</th><th>Contact</th><th>Channels</th><th>Lead status</th><th>Orders</th><th>Last interaction</th><th>Tags</th></tr></thead><tbody>{rows.map(r=><tr key={r._id} className="border-b border-white/6"><td className="p-4"><input type="checkbox" className="h-4 w-4"/></td><td><Link href={`/customers/${r._id}`} className="font-semibold text-white hover:text-[#68d2c8]">{r.name}</Link></td><td><div>{r.phone}</div><div className="text-xs text-slate-500">{r.email}</div></td><td>{(r.channels||[]).join(', ')||'—'}</td><td><StatusBadge value={r.lead_status||'None'}/></td><td>{r.orders_count||0}</td><td>{r.updated_at?new Date(r.updated_at).toLocaleDateString():'—'}</td><td>{(r.tags||[]).join(', ')||'—'}</td></tr>)}</tbody></table></div></DataState></div>}
